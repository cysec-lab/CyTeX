version: "3.8"
services:
  ubuntu-texlive-ja:
    image: ran350/cytex-texlive:latest
    # build: .
    command: sleep infinity
    volumes:
      - ../:/workdir
      - ./.latexmkrc:/root/.latexmkrc
      - ~/.gitconfig:/root/.gitconfig
    environment:
      SHELL: "/bin/bash"
